interface Point {
  x: number;
  y: number;
}

interface Box {
  x: number; // coordinates of top-left corner (x is towards right, y is towards bottom)
  y: number;
  width: number;
  height: number;
}
