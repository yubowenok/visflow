import { namespace } from 'vuex-class';

export const ns = {
  dataflow: namespace('dataflow'),
  message: namespace('message'),
  systemOptions: namespace('systemOptions'),
  panels: namespace('panels'),
  interaction: namespace('interaction'),
};
